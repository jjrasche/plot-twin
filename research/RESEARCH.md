# RESEARCH — live conclusions

| id | conclusion | status | scope | grounding |
|---|---|---|---|---|
| Q-001 | Three-tier inner loop: op-row constraint compile → coarse CP-SAT topology → seeded local search on exact polygons; LLM emits relational ops from a closed set, never coordinates (LLM-coordinates ≈ random in ablations) | ANSWERED(2026-08) | site-layout optimization, one parcel | [ANSWER](questions/Q-001-llm-solver-hybrid/ANSWER.md) |
| Q-002 | Terrain already solved free: QL2 LiDAR (~2.2 pts/m²) + USGS 1m DEM + 60cm 4-band NAIP cover the Delta Twp parcel; aerial-first typing pipeline; $0 one-day v1 experiment specced | ANSWERED(2026-08) | the home parcel, Delta Twp, Eaton Co MI | [ANSWER](questions/Q-002-agentic-capture/ANSWER.md) |
| Q-003 | Current scene3d FAILs at terrain scale (6.9fps @ 100K triangles; killers: per-frame sort + boxed assembly, not projection); rebuild = batched drawVertices inside Compose Canvas, effort M; GPU interop rejected (blinds captureToImage) | ANSWERED(2026-08) | factored-ui desktop; wasm/Android unverified | [ANSWER](questions/Q-003-scene3d-rebuild/ANSWER.md) |
| Q-005 | Earthworks proposal: one `regrade` verb (subject · ground-form · prose extent; surface is solved), cut/fill as an *earthwork ledger projection* not violations (shrink/swell factors carried with provenance — a 25% shrink turns a "balanced" job into 25,000 yd³ of borrow), spoil conserved by a hard invariant with `haul-off` as the named escape, terrain rows gain a surface id with solvers taking `(date, surface)`, purpose selects a rule bundle not a verb (NRCS terrace: 5:1 farmable vs 2:1 not) | PROPOSED(2026-08) | earthmoving design ops; awaiting owner ratification | [ANSWER](questions/Q-005-earthworks-design/ANSWER.md) |
| Q-006 | Every row is a replace at a named scope (never relative), so the projection cache never invalidates — it only lags; shipped a terrain cache keyed on folded-through seq (no schema change). Snapshots are derived+droppable, keyed by (projection, parameters, seq, fold version) or they silently compare two different folds. Ordering resolves conflicts but never detects them, and seq is ingest time, not world time. Truth rows are never compactable; codec then cold storage, never delete | ANSWERED(2026-08) | log folds, terrain projection; snapshot row itself awaits owner | [ANSWER](questions/Q-006-snapshot-and-lenses/ANSWER.md) |
| Q-004 | Sun+sky slice = line-sweep horizon lighting (the sweep IS the sunshed solver) + Grena3 sun position + Hosek-Wilkie dome, all per-vertex through the existing painter; splats are CAPTURE only (splat→mesh matured 2025-26: MILo et al.; consumer apps export meshes) — never the renderer; horizon maps got Fourier-compressed (HPG 2025), not replaced | ANSWERED(2026-08) | Skia/CPU-vertex pipeline; web receipts 2025-2026 | [ANSWER](questions/Q-004-light-sky-render/ANSWER.md) |
