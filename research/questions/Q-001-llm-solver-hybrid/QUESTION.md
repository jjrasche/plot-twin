---
id: Q-001
status: open
trigger: decision
spawned-by: D-003
feeds: D-003
---

# How should the LLM and classical solvers split the layout-optimization work?

What op vocabulary should the LLM emit, which inner-loop optimizer fits discrete site
layout, and where does the LLM-prunes/solver-grinds seam go? Open until the op pipeline
build (TASKS Now) consumes the answer.
