# Future

- AR walkthrough: phone WebXR/ARCore, model anchored via GPS + manual reference-point alignment (outdoor registration is the hard part).
- Cinematic beauty pass: illuminant-style video skin over state renders.
- Water flow visual: particles advected along D8 arrows.
- Adaptive grid (quadtree) if 10cm uniform grid becomes a limit.
- Capture agent: photos + Google Maps + GPS → world-state entities, agentic compare-refine loop.
